# Web Audio Route Map

Live: https://web-audio-route-map.sociobot.in — built by the Param Factory (`library-npm`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
